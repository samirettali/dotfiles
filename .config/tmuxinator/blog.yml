name: blog
root: ~/dev/blog

windows:
  - servers:
      layout: even-vertical
      panes:
        - hugo server --verbose --watch --buildDrafts
  - editor:
      layout: even-vertical
      panes:
        - cd content/writeups && vim .
